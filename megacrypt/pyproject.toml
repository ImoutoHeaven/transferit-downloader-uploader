[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "megacrypt"
version = "0.1.0"
description = "MEGA/transfer.it file encryption (AES-CTR + chunk MACs) as a native Python extension"
requires-python = ">=3.8"

[tool.maturin]
module-name = "megacrypt"
